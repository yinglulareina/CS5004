public class Power {
    public static void main(String[] args){
        long result = (long) Math.pow(2, 31);
        System.out.println(result);

        /* Task F: Difference between Java and Python:
        1. Java must declare variable type (like long), Python no need.
        2. Java uses ";" at the end and "{}" for code, Python uses space/indent.
        3. Java needs compile to bytecode, but Python is interpreted.
        */
    }
}
